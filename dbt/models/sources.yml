version: 2

sources:
  - name: raw
    database: datatalent-simplon
    schema: raw
    tables:
      - name: communes_raw
        description: "Référentiel communes (CSV data.gouv) — colonnes chargées en STRING pour préserver 2A/2B et zéros initiaux."
      - name: raw_offres_team
        description: "Offres brutes FT + Adzuna (payload_json) — alimenté par le job Cloud Run."
      - name: offres_enriched_team
        description: "Offres enrichies Sirene (API) — MERGE depuis le job Cloud Run."

